[package]
name = "kernel"
version = "0.4.0"
edition = "2018"
autobins = false

[lib]
name = "kernel"
path = "src/main.rs"
crate-type = ["staticlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
